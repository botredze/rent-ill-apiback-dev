export enum UserReactionStatusType {
    Pending = 'PENDING',
    Accepted = 'ACCEPTED',
    Declined = 'DECLINED',
}

export enum UserReactionType {
    Accepted = 'ACCEPTED',
    Declined = 'DECLINED',
}
