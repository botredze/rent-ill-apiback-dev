export enum StatusType {
    Active = 'ACTIVE',
    Disabled = 'DISABLED',
    Deleted = 'DELETED',
}

export enum SignyDocumentStatusTypes {
    Active = 'ACTIVE',
    Disabled = 'DISABLED',
    Deleted = 'DELETED',
    ARCHIVED = 'ARCHIVED',
    Draft = 'DRAFT',
}

export enum SignyContactStatusTypes {
    Active = 'ACTIVE',
    Disabled = 'DISABLED',
    Deleted = 'DELETED',
    Archived = 'ARCHIVED',
}

export enum PostStatusType {
    Published = 'PUBLISHED',
    Archived = 'ARCHIVED',
    Deleted = 'DELETED',
}

export enum InvitationStatusTypes {
    Pending = 'PENDING',
    Sent = 'SENT',
    Registred = 'REGISTRED',
}

export enum RequestType {
    Company = 'COMPANY',
    Branch = 'BRANCH',
    Registration = 'REGISTRATION',
}

export enum ServiceTypes {
    Signition = 'SIGNITION',
    Rentil = 'RENTIL',
    Boards = 'BOARDS',
}

export enum SubscriptionStatusTypes {
    Active = 'ACTIVE',
    Disabled = 'DISABLED',
    Deleted = 'DELETED',
    Processing = 'PROCESSING',
}

export enum FeatureStatusTypes {
    Active = 'ACTIVE',
    Disabled = 'DISABLED',
    Deleted = 'DELETED',
    Archived = 'ARCHIVED',
}

export enum CouponStatusType {
    Used = 'USED',
    Deleted = 'DELETED',
}

export enum SubscriptionStatusType {
    Incomplete = 'INCOMPLETE',
    IncompleteExpired = 'INCOMPLETE_EXPIRED',
    Trialing = 'TRIALING',
    Active = 'ACTIVE',
    PastDue = 'PAST_DUE',
    Canceled = 'CANCELED',
    Unpaid = 'UNPAID',
}
